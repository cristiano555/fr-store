import styled from 'styled-components'
import { autoSize } from '../../helpers/styles'
import {
    colors,
    fonts,
    fontsSizes,
    fontsWeights,
    media,
    mainContainer
} from '../../helpers/styles'

export const HeadlineContainer = styled.div`
    text-align: center;
    padding: ${autoSize(30, 'all')}
`
